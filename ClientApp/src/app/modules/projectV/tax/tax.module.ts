import { NgModule } from '@angular/core';
import { TaxRoutingComponent, TaxRoutingModule } from './tax.routing';


@NgModule({
  declarations: [TaxRoutingComponent],
  imports: [TaxRoutingModule],
  providers: [],
  exports: []
})
export class TaxModule {
 
}
